// Global state registry that bridges GSAP ScrollTrigger timeline (DOM) and R3F rendering loop (Canvas)
export const scrollState = {
  meshScale: 1.0,
  meshRotationSpeed: 1.0,
  rimColor: "#ff4500", // Cinematic volcanic orange
  cameraZ: 1.25,       // Default end position after hero transition
  materialTransmission: 0.25,
};
